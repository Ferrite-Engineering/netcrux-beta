{
  "_comment": "Expected ProFsmDetectionService.detect() result for candidate_register.v — the 16-bit register exceeds the v1 width cap (8 bits) and surfaces as a candidate. The user can force detection via the FsmDetectionResultsDialog's per-row 'Force detection' action.",
  "expectedFsmCount": 0,
  "expectedCandidateCount": 1,
  "candidates": [
    {
      "stateRegisterName": "state_q",
      "diagnosticContains": "exceeds the v1 upper bound"
    }
  ]
}
